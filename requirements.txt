# ── Jarvis Vocabulary Assistant — requirements.txt ───────────────────────────
# Install: pip install -r requirements.txt

# UI
streamlit>=1.35.0

# LLM / AI brain
langchain>=0.2.0
langchain-community>=0.2.0       # Ollama integration
langchain-core>=0.2.0
ollama>=0.2.0                    # Python client for local Ollama server

# Audio — STT
SpeechRecognition>=3.10.0
PyAudio>=0.2.14                  # Microphone access (see note below)

# Audio — TTS (choose one; both listed for easy switching)
gTTS>=2.5.1                      # Online, lightweight — DEFAULT
pygame>=2.5.2                    # Audio playback for gTTS MP3s
# kokoro>=0.9.2                  # Offline, high-quality — uncomment to enable
# soundfile>=0.12.1              # Required by Kokoro
# sounddevice>=0.4.7             # Required by Kokoro

# Dictionary / HTTP
requests>=2.31.0

# ── Notes ────────────────────────────────────────────────────────────────────
# PyAudio on Linux:  sudo apt install portaudio19-dev && pip install PyAudio
# PyAudio on macOS:  brew install portaudio && pip install PyAudio
# PyAudio on Win:    pip install pipwin && pipwin install PyAudio
#
# Ollama must be running separately:
#   Install → https://ollama.com/download
#   Pull model → ollama pull llama3          (or mistral, phi3, gemma2, etc.)
#   Start server → ollama serve              (default: localhost:11434)
